@echo off
setlocal
set "SCRIPT_DIR=%~dp0"
powershell -NoProfile -ExecutionPolicy Bypass -File "%SCRIPT_DIR%deaddrop-installer.ps1" %*
if errorlevel 1 (
  echo.
  echo DeadDrop Desktop install failed.
  pause
  exit /b 1
)
echo.
echo DeadDrop Desktop install finished.
pause
